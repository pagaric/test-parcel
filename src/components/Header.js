const Header = () => {
    const template = `
        <header>
            <h1>Mon application avec Parcel</h1>
            <p>Du code passe partout pour un workflow JS avec Parcel</p>
        </header>
    `;

    return template;
}

export default Header;